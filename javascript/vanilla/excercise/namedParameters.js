const f = ({
    boolean,
    arr: [ ...func ],
    float,
    object: {
        name,
        value
    },
    string
}) => {
    boolean ? {
        arr(string)
    } : {
        "muka"
    }
}

console.log(f({}));